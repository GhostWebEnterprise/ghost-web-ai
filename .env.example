# Server-side AI Gateway configuration
# Do NOT prefix secrets with VITE_.
AI_GATEWAY_API_KEY=
AI_GATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1

# Optional provider keys used by the Ghost planner fallback chain.
# Keep all secrets server-side.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
SAMBANOVA_API_KEY=
